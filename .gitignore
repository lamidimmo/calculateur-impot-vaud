# intermediate Excel extraction (not part of the deliverable)
/igi_dump/

# local Claude Code settings (per-user)
.claude/settings.local.json

# Projets distincts hébergés dans le même dossier (dépôts git séparés)
/Dev foncier/
/klova-crm/
/premiere-cowork-bridge/
/CRM Aquéreur/
